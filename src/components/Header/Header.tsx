import { FC } from 'react';

import { Container } from '@/components/Container/Container';

export const Header: FC = () => {
  return (
    <header>
      <Container>
        <h1>Билетопоиск</h1>
      </Container>
    </header>
  );
};
